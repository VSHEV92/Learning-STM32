diff --git a/eeprom_mem_dma/Core/Src/main.c b/eeprom_mem_dma/Core/Src/main.c
index 3b16ffe..314f593 100644
--- a/eeprom_mem_dma/Core/Src/main.c
+++ b/eeprom_mem_dma/Core/Src/main.c
@@ -21,7 +21,8 @@
 
 /* Private includes ----------------------------------------------------------*/
 /* USER CODE BEGIN Includes */
-
+#include "stdio.h"
+#include "string.h"
 /* USER CODE END Includes */
 
 /* Private typedef -----------------------------------------------------------*/
@@ -31,6 +32,9 @@
 
 /* Private define ------------------------------------------------------------*/
 /* USER CODE BEGIN PD */
+#define EEPROM_ADDR 0xA0
+#define BASE_ADDR 0x00
+#define BUF_SIZE 30
 
 /* USER CODE END PD */
 
@@ -62,7 +66,24 @@ static void MX_USART2_UART_Init(void);
 
 /* Private user code ---------------------------------------------------------*/
 /* USER CODE BEGIN 0 */
+int __io_putchar(int ch) {
+  HAL_UART_Transmit(&huart2, (uint8_t *)&ch, 1, HAL_MAX_DELAY);
+  return ch;
+}
+
+volatile uint32_t tx_completed = 0;
+volatile uint32_t rx_completed = 0;
+
+uint8_t tx_buffer[BUF_SIZE] = "Hello";
+uint8_t rx_buffer[BUF_SIZE];
 
+void HAL_I2C_MemTxCpltCallback(I2C_HandleTypeDef *hi2c) {
+  tx_completed = 1;
+}
+
+void HAL_I2C_MemRxCpltCallback(I2C_HandleTypeDef *hi2c) {
+  rx_completed = 1;
+}
 /* USER CODE END 0 */
 
 /**
@@ -105,6 +126,25 @@ int main(void)
   /* USER CODE BEGIN WHILE */
   while (1)
   {
+	printf("Write to EEPROM: %s\n", tx_buffer);
+
+	while( HAL_I2C_IsDeviceReady(&hi2c1, EEPROM_ADDR, 1, HAL_MAX_DELAY) != HAL_OK ) {}
+
+    HAL_I2C_Mem_Write_DMA(&hi2c1, EEPROM_ADDR, BASE_ADDR, 1, tx_buffer, strlen((char*)tx_buffer)+1);
+
+    while(!tx_completed){}
+    tx_completed = 0;
+
+	while( HAL_I2C_IsDeviceReady(&hi2c1, EEPROM_ADDR, 1, HAL_MAX_DELAY) != HAL_OK ) {}
+
+	HAL_I2C_Mem_Read_DMA(&hi2c1, EEPROM_ADDR, BASE_ADDR, 1, rx_buffer, strlen((char*)tx_buffer)+1);
+
+	while(!rx_completed){}
+	rx_completed = 0;
+
+	printf("Read from EEPROM: %s\n", rx_buffer);
+
+	HAL_Delay(1000);
     /* USER CODE END WHILE */
 
     /* USER CODE BEGIN 3 */
